import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test', (WidgetTester tester) async {
    // Placeholder test — the app requires platform channels and network access
    // that are not available in the widget test environment.
    expect(true, isTrue);
  });
}
